<PUBLIC:COMPONENT lightWeight="true">


<PUBLIC:PROPERTY NAME="placeholder" PUT="placeholderSet" GET="placeholderGet" /></PUBLIC:COMPONENT>

<SCRIPT LANGUAGE="JScript">
var noop = function(){};
var doProp = function(name, action, arg){
	 if(!jQuery.webshims.magicProps[name]){return undefined;}
	 return jQuery.webshims.magicProps[name][action](element, arg);
};

var get = function(name){
	return doProp(name, 'getter');
};
var set = function(name, arg){
	return doProp(name, 'setter', arg);
};



//placeholder
var placeholderSet = function(val){
	set('placeholder', val);
};
var placeholderGet = function(){
	return get('placeholder', element);
};
</SCRIPT>